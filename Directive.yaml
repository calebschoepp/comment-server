identifier: com.calebschoepp.comment-server
appVersion: v0.0.1
atmoVersion: v0.2.0

handlers:
  - type: request
    resource: /platform/:platform/url/:url
    method: GET
    steps:
      - fn: validate
      - fn: fetch-html
      # - fn: parse